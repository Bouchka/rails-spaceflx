<%= simple_form_for @space do |f| %>
  <%= f.input :name %>
  <%= f.input :capacity %>
  <%= f.input :area, collection: Space::ZONES %>
  <%= f.input :price_per_day %>
  <%= f.input :description %>
  <%= f.input :photo, as: :file %>
  <%= f.button :submit, class: 'btn btn-primary' %>
<% end %>
